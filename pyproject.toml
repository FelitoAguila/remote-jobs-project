[project]
name = "remote-jobs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "prefect>=3.7.0,<3.8",
    "dlt[bigquery]>=1.0",
    "prefect-gcp==0.6.2",
    "google-cloud-bigquery",
]
