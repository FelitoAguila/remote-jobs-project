[project]
name = "remote-jobs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "prefect[gcp]>=3.7.0",
    "dlt[bigquery]>=1.0",
    "google-cloud-bigquery",
]
